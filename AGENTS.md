# Agents Entry Point

Use `AGENT.md` as the canonical agent guide for this repository. All agent workflows follow global `.claude` rules (`~/.claude/rules/workflow.md`, `~/.claude/rules/documentation.md`, `~/.claude/rules/self-review.md`).

This file exists because some coding agents look for `AGENTS.md` by convention.

